- var lg = (i18n ? i18n.language : 'fr');
doctype
html(lang= lg)
  head
    meta(charset="utf-8")
  body
    block main
    if initialState
      script(type="text/javascript").
        window.__INITIAL_STATE__ = !{initialState};
  script(type="text/javascript", src="/src/dist/main.bundle.min.js", charset="utf-8")
  link(rel="stylesheet",href="/src/dist/main.min.css")
